{
  "name": "maharishi-meditation",
  "version": "1.0.0",
  "private": true,
  "description": "Maharishi Meditation app and backend",
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd backend && npm install && npm run build",
    "postinstall": "cd backend && npm install"
  },
  "engines": {
    "node": "18"
  }
}
